<div class="container text-center">
  <p>
    <%= link_to "Admin", admin_path %> <%= " > #{params[:action].capitalize} " unless params[:action].eql?("index") %>
  </p>
  <div class="btn-group">
    <%= render "admin/link", resorce: Post %>
    <%= render "admin/link", resorce: Comment %>
    <%= render "admin/link", resorce: User %>
  </div>
</div>